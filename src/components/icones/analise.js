import React from 'react';
import { Icon } from '@material-ui/core';

const Logo = () => (
  <Icon>
    <img src="/images/analise.svg" height={22} width={25} alt="brasil" />
  </Icon>
);

export default Logo;
