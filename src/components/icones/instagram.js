import React from 'react';
import { Icon } from '@material-ui/core';

const Logo = () => (
  <Icon>
    <img src="/images/instagram.svg" height={25} width={25} alt="brasil" />
  </Icon>
);

export default Logo;
